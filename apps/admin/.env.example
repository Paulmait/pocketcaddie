# SliceFix AI - Admin Portal Environment Variables
# Copy to .env.local and fill in values
# NEVER commit .env.local to version control

# Supabase Configuration
# IMPORTANT: This portal uses SERVICE_ROLE_KEY for admin operations
# This key is ONLY used server-side (Server Components, Server Actions, Route Handlers)
# It is NEVER exposed to the browser

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# SERVICE ROLE KEY - Server-side only!
# This key bypasses RLS and has full database access
# NEVER use this in client components
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin Portal Settings
ADMIN_PORTAL_URL=http://localhost:3001
